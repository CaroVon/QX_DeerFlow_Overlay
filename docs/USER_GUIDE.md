# QX Studio 使用指南与测试要点（v0.5）

> 入口（cloudflare 已更新）：**https://logical-temporal-baltimore-latin.trycloudflare.com**
> （本机恒定可用 http://localhost:2026；quick tunnel 域名随 cloudflared 重启漂移，以本文档最新地址为准）

## 〇、快速上手（新用户 5 分钟路径）

1. 打开入口 → 「Sign up」注册：填邮箱、密码、**邀请码**（向管理员索取）→ 自动进入工作台
2. 左侧「模板市场」→ 采用「🎯 关键词黄金路径」或任一「设计」模板
3. 左侧「Agents」→ 选刚采用的 agent 开聊：说"我想要便携咖啡机的设计关键词"
4. agent 确认采集消耗 → 提炼关键词 → 右侧面板出现**可编辑关键词卡** → 选设计风格 → 点「用关键词生图」
5. 1-6 分钟后右侧「生图记录」出图；到「QX Studio → 资产库」点「导出作品 ZIP」拿到作品

---

## 一、功能使用指南

### 1. 账号与邀请码
- 注册需邀请码（单次有效，先到先得）；登录会话在服务重启后保持
- 管理员在「用量额度」页底部管理区：生成邀请码（可批量、可备注批次）、查看全员额度、发放/扣回三类额度
- 每个用户注册赠送：**50 万 LLM tokens / 20 张生图 / 50 采集 credits**

### 2. 聊天与 Agent
- 输入框下方快捷按钮：[QX 全流程][研究][矩阵][生图]；输入 `/` 出技能补全
- **模板市场**（侧栏）：12 套官方模板（组合/研究/设计/演示四栏），一键采用即成为你的 agent（重名自动加后缀），去「Agents」开聊
- 右侧「任务面板」按钮：进度/审批门/生图记录/关键词卡都在这里，可收起展开

### 3. 关键词黄金路径（核心场景）
说"我想要 XX 产品的设计关键词" → agent 真实采集（先确认消耗）+ 搜索 → 5 组关键词（设计/功能/外观/人群/场景）落为右侧面板**可编辑关键词卡**：
- 直接增删词后「保存」
- 「用关键词生图」→ 选**设计风格**（极简棚拍/工业手绘/生活方式/3D C4D/水彩/哑光 CMF/概念科幻/拼版版式/自动）→ 独立生图 或 挂到某项目（同时进项目设计库）
- agent 会主动询问下一步；不想采集可说"用 mock"

### 4. 生图与资产库（QX Studio → 资产库组块）
- 聊天生图、手动上传（图片/文档 ≤20MB）统一入库；**只看得到自己的资产**
- 每张图：预览放大、单独下载、挂载/取消挂载到项目（跨场景联动）、删除
- 「导出作品 ZIP」：打包本人全部已完成生图（作品集图片包）
- 筛选：全部 / 生图 / 文档 / 未挂载

### 5. 全流程任务（qx-full）
- 说"启动 QX 全流程：XX 产品"或点 [QX 全流程]：研究→MOD 矩阵→策略/PRD→设计→演示→PPT（约 30 分钟）
- 右侧面板实时节点进度；资料/大纲审核门到达时出现摘要卡可勾选批准
- Studio 页顶部「自动过门」开关：放手全自动（产物仍可回看）
- **终止任务**：面板内按钮，状态变灰色「已取消」（非失败），可放心终止
- 「任务资产 / 下载」：逐文件预览+单独下载；右上角「下载全部 ZIP」（二次下载秒回，已缓存）

### 6. 用量额度（侧栏「用量额度」）
- 三类余额卡 + 近 30 天消耗 + 流水明细（每一笔消耗/发放/退款可追溯）
- 「同步 LLM 用量」把对话消耗入账；额度不足的操作会被友好拦截（提示联系管理员）
- 管理员附加：全员额度总览、按用户发放（正数发放/负数扣回）、邀请码生成

---

## 二、测试要点（逐项勾选，记录 编号+现象）

### A. 账号与邀请码（新）
- [ ] A1 无邀请码注册被拒（403 提示）
- [ ] A2 管理员在用量页生成 2 枚邀请码，页面可见码值
- [ ] A3 持码注册成功并自动登录进工作台
- [ ] A4 同一码二次注册被拒
- [ ] A5 新用户注册赠送三类额度正确（50 万/20/50）
- [ ] A6 旧会话（管理员）与新用户互不影响，刷新不掉线

### B. 模板市场（新）
- [ ] B1 市场页四栏筛选正常，12 套模板卡片完整
- [ ] B2 一键采用「关键词黄金路径」→ Agents 列表出现且可开聊
- [ ] B3 采用的 agent 工具边界正确：设计类模板聊天中**不能**触发全流程提交
- [ ] B4 重复采用同款自动加后缀，不报错

### C. 关键词路径与风格（新）
- [ ] C1 说"我想要 XX 的设计关键词" → agent 先确认采集消耗再执行
- [ ] C2 关键词卡出现在右侧面板，5 组可编辑、保存生效
- [ ] C3 生图对话框 9 档设计风格可选，出图风格符合所选
- [ ] C4 「独立生图」与「挂载到项目」两条路径都出图（1-6 分钟）
- [ ] C5 说"用 mock 采集"不消耗 credits 也能出关键词

### D. 计费与账单（新）
- [ ] D1 生图一次余额 -1（用量页流水可见）
- [ ] D2 余额置零后生图被拦截（402 友好提示），管理员发放后恢复
- [ ] D3 生成失败自动退款（流水有退补记录）
- [ ] D4 「同步 LLM 用量」后对话消耗入账，数字合理
- [ ] D5 管理员发放/扣回即时反映到用户余额
- [ ] D6 真实采集（确认后）扣 rainforest credits；mock/回放不扣

### E. 多用户隔离（新）
- [ ] E1 用户 A 的资产库/任务列表看不到用户 B 的任何内容
- [ ] E2 用户 A 的生图记录、关键词卡、用量流水均与 B 互不可见
- [ ] E3 管理员可全览资产与全员额度

### F. 内容安全（新）
- [ ] F1 生图 prompt 含明显违禁词 → 422 拦截并提示调整
- [ ] F2 设计组块内同样被拦截

### G. 回归（v0.4 修复项抽测）
- [ ] G1 生图链接可直接点开看图（B3）
- [ ] G2 任务终止 → 灰色「已取消」徽章，节点停止（B9）
- [ ] G3 资产弹窗 96% 宽大窗（C3）；任务资产逐文件下载（C4）
- [ ] G4 MOD 矩阵详情主图正常显示（D4）
- [ ] G5 点击画布中心 PPT 打开演示详情（D8）
- [ ] G6 记忆图谱节点 icon 为品类色、点击出解释（D7）
- [ ] G7 聊天说"生成一个便携咖啡机概念图" → 独立出图进生图记录（B3/B8）

### H. 边界与已知事项
- 真实 Rainforest 采集消耗平台 credits，agent 调用前必须确认；mock 免费演示
- 全流程真实运行约 30 分钟（审核模式更久）；PPT 并发 6
- 旧演示编辑器（:8000）仅承载重型编辑，公网隧道未暴露，本机可用
- quick tunnel 域名随重启漂移；正式开放用户前将切换固定域名

---

*v0.5 变更：新增邀请码注册、模板市场、关键词黄金路径（风格预设）、三类额度计费与账单页、多用户隔离、内容安全、作品 ZIP 导出、admin 邀请码/额度管理。管理员账号：admin@deerflow.qxdev.com（联系管理员获取邀请码与额度）。*
