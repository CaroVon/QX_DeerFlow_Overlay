# QX Studio 用户测试手册（v0.4）

入口：https://podcasts-spirit-debian-farmers.trycloudflare.com（本机 http://localhost:2026）
登录：admin@deerflow.qxdev.com / QxAdmin2026!

按序号逐条测试，遇到问题记录编号+现象反馈。
v0.3 → v0.4 变更概要：v0.3 全部 ERROR 已修复；新增独立资产库、关键词黄金路径、任务终止按钮；
agent 工具边界按组隔离（qx-designer 不再能触发全流程）；全流程性能优化（采集/生图并发，目标 ≤30 分钟）。

## A. 登录与会话

* \[ OK ] A1 打开入口，登录后自动进入工作台，会话在服务重启后仍保持（不掉线）
* \[ OK ] A2 头部品牌显示 "QX Studio"

## B. 聊天驾驶舱

* \[ OK ] B1 新建聊天，输入框下方可见 \[QX 全流程]\[研究]\[矩阵]\[生图] 按钮，点击出现对应 /qx- 命令，回车可发送
* \[ OK ] B2 输入 "/" 出现技能补全列表，含 qx-full / qx-image / qx-matrix / qx-research / style
* \[ FIXED→复测 ] B3 直接说"生成一个便携咖啡机产品概念图"→ 独立生图（不建任务），返回的图片链接可直接点开；图片进入右侧面板「生图记录」并持久保存
  - 修复：生图产物落库（qx_assets）+ 文件落盘，重启不丢；URL 同源可点开
* \[ FIXED→复测 ] B4 说"采集 wireless mouse 竞品数据"→ agent 先确认 credits 消耗，确认后走 Rainforest 真实采集返回摘要
  - 修复：gateway 进程桥接 RAINFOREST_API_KEY；策略改为「默认真实源，采集前必须确认」
* \[ UNLOCKED ] B5 提交全流程（"启动 QX 全流程：XX 产品"）→ 右侧面板自动打开并切到"QX 任务"tab，显示节点进度
  - 本轮已解锁：其他模块修复完毕，可进行全量测试（预计 ≤35 分钟；关注分阶段耗时输出）
* \[ OK ] B6 面板顶部"任务面板"按钮：再点收起、再点展开
* \[ OK ] B7 从左侧 Agents 选 qx-designer 对话 → 聊天头部同样有"任务面板"按钮
  - 新增：qx-designer 现在只能看到生图工具（工具组隔离），不再可能误触发全流程
* \[ FIXED→复测 ] B8 生图完成后，任务面板出现"生图记录"图片网格（无任务时常驻显示），点击可放大
* \[ FIXED→复测 ] B9 任务运行中点面板内新增的「终止任务」按钮 → 状态变灰色"已取消"（非失败），节点停止
  - 修复：新增 cancelled 终态（与失败严格区分），取消不可被重试复活
* \[ UNLOCKED ] B10 资料门到达时，面板出现 MOD 摘要卡 + 资料勾选列表，可勾选后批准
* \[ OK ] B11 输入 "/style" → 四档风格切换，新开对话仍生效
* \[ NEW ] B12 关键词黄金路径：对 qx-researcher 说"我想要便携咖啡机的设计关键词" → agent 确认后采集+搜索 → 右侧面板出现**可编辑关键词卡**（5 组，可增删词）→ 卡片有「用关键词生图」按钮（独立生图或挂项目）→ agent 会主动询问下一步

## C. Studio 工作台（左侧 QX Studio）

* \[ OK ] C1 顶部项目选择框：下拉切换任务，选项带 \[状态] 前缀（新增"已取消"状态）
* \[ OK ] C2 画布居中：PPT 当前页在中心，资产组块环绕（现为 9 组块，新增"资产库"）
* \[ FIXED→复测 ] C3 点击任意资产组块 → 居中大窗口（96% 宽）展开详情
  - 修复：样式冲突导致弹窗恒 512px 宽
* \[ FIXED→复测 ] C4 点"任务资产 / 下载"→ 弹出资产清单窗口：逐文件预览+单独下载；右上角"下载全部 ZIP"（首次打包后缓存，二次秒下）

## D. 资产模块（画布组块逐个点开）

* \[ OK ] D1 资料源：门到达时展示完整审核视图；非门期显示状态说明
* \[ OK ] D2 市场研究：TAM/SAM/SOM 四格 + 竞品威胁卡 + 痛点/差异化 + 引用源
* \[ OK ] D3 策略/PRD：定位条 + 用户画像 + P0-P2 功能矩阵 + 路线图 + PRD 手风琴
* \[ FIXED→复测 ] D4 MOD 矩阵：详情顶部显示矩阵主图（价格带四区图），下方产物文件链接可下载
  - 修复：前端补主图渲染；后端主图下载失败不再永久跳过（6h 冷却后重试，回放可补采）
* \[ FIXED→复测 ] D5 关键词：5 组编辑器保存；"用新词生图"正常出图并回显
  - 修复：接口响应嵌套解析错误导致的 items/undefined 404
* \[ OK ] D6 设计：缩略图、编辑/生图/版本历史、智能拆解组件（弹窗已变大，加载更醒目）
* \[ FIXED→复测 ] D7 记忆图谱：本任务/全局切换；节点带品类色 icon；点击节点出解释（摘要+关系列表）；空态"立即抽取"→ 抽取中轮询到完成自动刷新
* \[ FIXED→复测 ] D8 演示/PPT：点击居中 PPT 当前页 → 打开演示详情（DSL 翻页/页级返工/PPTX 下载）；"在编辑器中打开"按当前主机名自动推导旧 SPA 地址
* \[ NEW ] D9 资产库（新组块）：独立生图/上传文档统一浏览；支持上传、删除、挂载/取消挂载到项目（跨场景联动）；未挂载视图只看独立资产

## E. 边界与已知事项

* 真实 Rainforest 采集消耗 credits：**默认真实源，agent 调用前必须先确认**；mock 仅在明确要求快速演示时使用
* 全流程目标 ≤30 分钟（保质量）：采集 4 并发、生图并发 6；smoke 含全链路断言
* 旧 SPA（:8000）承载演示页重型编辑；新界面为只读渲染
* quick tunnel 域名随 cloudflared 重启漂移；本地 2026 恒定可用
* F1 第二阶段（qx-full 重构为独立 agent 的可插拔编排）为后续工作，本轮已完成工具组/技能白名单隔离

## F. 本轮架构变更（对照 v0.3 F 节）

1. agent 拆分先行已落地：qx-studio MCP 服务器下线，全部能力迁为声明式工具组
   （qx-research / qx-matrix / qx-knowledge / qx-design / qx-pipeline），agent 按组+技能白名单隔离；
   独立生图不再依赖任何任务（产物入资产库）
2. 性能：未引入 Rust（瓶颈为 IO 等待）——采集并发化 + 搜索并行 + 生图并发 6 + LLM 节奏不变；
   full_pipeline_test.py 输出分阶段耗时供回归对照
